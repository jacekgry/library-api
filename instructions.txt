To build the project use following command: mvn clean package

After building the application run following command to start it: java -jar target/cognifide-0.0.1-SNAPSHOT.jar --app.datasource=static/books.json
Default datasource is static/books.json.
Application runs on port 8080.